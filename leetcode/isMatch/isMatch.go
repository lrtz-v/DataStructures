package isMatch

func isMatch(s string, p string) bool {

	// pIndex, sIndex := 0, 0
	// for sIndex < len(p) {

	// 	pTmp, sTmp := p[pIndex], s[sIndex]
	// 	if pTmp == '.' {
	// 		sIndex++
	// 		pIndex++
	// 		continue
	// 	} else if pTmp == '*' {
			
	// 	} else 


	// }
	return true
}
